import data # imports the data.py file

# This would contain the User Interaction and User Interface

# Sample
studentDict = data.getStudentDictionary(input("Name: "), input("Program: "))
data.addToFile(studentDict)
